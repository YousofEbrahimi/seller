# Deny all direct web access to downloadable product files.
# Files are only served through the bot via Telegram API.
# Lighttpd/nginx users: see README for equivalent rules.
Order Allow,Deny
Deny from all

<IfModule mod_rewrite.c>
RewriteEngine On
RewriteRule ^.*$ - [F,L]
</IfModule>
